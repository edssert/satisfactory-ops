---
/** 옛 주소. 「Codex」를 「도감」으로 바꾸면서 /dex/ 로 옮겼다 */
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const to = `${base}/dex/reference/`;
---
<meta http-equiv="refresh" content={`0; url=${to}`} />
<link rel="canonical" href={to} />
<p><a href={to}>새 주소로 이동</a></p>
