# 이 저장소는 장시간의 조사·구현·시각 검증을 한 작업에서 이어 간다.
# GPT-5.6 Sol의 원격 모델 계약과 중간 추론 강도를 사용한다.
# 복잡한 판단만 필요한 범위에서 깊게 보고, 일반 구현은 산출물과 짧은 피드백 루프를 우선한다.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

[features]
prevent_idle_sleep = true
