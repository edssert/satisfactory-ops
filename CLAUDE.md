# Claude Code 호환 진입점

이 저장소의 AI 운영 규칙은 [`AGENTS.md`](AGENTS.md)가 유일한 정본이다. 이 파일에 별도 규칙을 추가하지
않는다.

작업 시작 순서:

1. `AGENTS.md`에서 실행 명령, 데이터 안전 규칙과 검증 절차를 읽는다.
2. `satisfactory-ops-vault/PROJECT-HUB.md`에서 제품 문서 구조를 읽는다.
3. 작업에 필요한 `PRODUCT`, `ENGINEERING`, `DESIGN`, `ASSETS`, `DECISIONS`, `ROADMAP`만 선택해 읽는다.
4. 외부 사실은 `satisfactory-ops-vault/docs/research/README.md`의 재조사 계약을 따른다.

두 파일이 어긋나면 `AGENTS.md`와 사용자의 최신 지시를 따른다.
