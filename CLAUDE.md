# satisfactory-ops — 개발 지침

Satisfactory(게임) **공장 설계 플레이북** 웹앱. 이 문서는 이 저장소에서 작업하는 Claude의 운영 지침이다.

## 이 프로젝트가 무엇인가

기존 Satisfactory 도구는 전부 **계산기**다 — "강화철판 5/분에 기계 몇 대?"는 답해주지만 "지금 뭘 해야 하는지", "지금 이 배치가 왜 T6에 문제가 되는지"는 답하지 않는다. 이 앱은 그 빈틈을 채운다.

한 문장: **마일스톤 단위로 "지금 할 일"과 "지금 남겨둬야 할 것"을 알려주는, 진행 상황을 아는 플레이북.**

계산기 기능도 있지만 그것은 부가물이다. 계산기를 더 잘 만드는 경쟁은 하지 않는다.

## 절대 규칙

### 1. 수치는 출처 없이 쓰지 않는다
게임 수치(레시피 비율, 전력, 처리량, 노드 순도)를 기억으로 쓰지 마라. 반드시 검증하고 출처를 남겨라. 우선순위:

1. 게임 배포 데이터 (`Docs.json`) — 정본
2. 공식 위키 `satisfactory.wiki.gg`
3. 커뮤니티 합의 (여러 출처가 일치)
4. 단일 출처 주장 → `disputed`로 표시하고 앱 핵심 데이터로 쓰지 않는다

모든 사실에는 `source`와 `confidence`(`verified` / `consensus` / `disputed`)를 붙인다. 근거는 `docs/research/`에 남긴다.

### 2. 이견을 숨기지 않는다
출처가 갈리면 양쪽을 다 쓰고 어느 쪽이 왜 더 믿을 만한지 밝힌다. 합의된 척하지 마라. 실제로 커뮤니티 의견이 갈리는 주제가 여럿 있다 (메인 버스 유용성, 북부 숲 지형 난이도 등).

### 3. "왜"를 항상 붙인다
이 앱의 차별점은 학습 레이어다. 규칙만 주지 말고 근거를 준다. 매니폴드를 쓰라고만 하지 말고, 왜 로드 밸런서보다 나은지, 대신 무엇을 포기하는지 설명한다. 초보가 처음 만나는 용어는 그 시점에 설명한다 (`src/data/glossary.json`).

### 4. 게임 자산 취급
`assets/` 하위 이미지는 Coffee Stain Studios 자산이며 공식 위키에서 가져왔다. MIT 라이선스 대상이 아니다. 출처 표기를 유지하고, 새 이미지를 추가할 때도 같은 원칙을 지킨다. 상업적 사용·게임사 사칭 금지.

### 5. 저장소 위치
이 저장소는 `C:\Dev\satisfactory-ops`에 있다. **OneDrive 안으로 옮기지 마라** — `.git` 동기화가 저장소를 깨뜨린다. 워크스페이스 규칙(`00.Workspace\CLAUDE.md`)에 명시된 사항이다. 자세한 배경은 `docs/adr/0002-repo-location.md`.

## 언어 정책

- 본문·UI: 한국어
- 게임 용어: **한글(영문) 병기**. 예: `강화 철판(Reinforced Iron Plate)`, `피복 산업용 빔(Encased Industrial Beam)`
  - 이유: 사용자가 인터랙티브 맵·위키·영문 공략과 대조할 수 있어야 한다
  - 첫 등장 시 병기, 같은 문단 내 반복은 한글만
- 코드 식별자·파일명·커밋 메시지: 영어
- 문서(`docs/`): 한국어. ADR도 한국어

## 문서 체계

| 문서 | 목적 | 언제 갱신 |
|---|---|---|
| `docs/PRD.md` | 무엇을 왜 만드는가 — 사용자, 문제, 성공 기준 | 제품 방향이 바뀔 때 |
| `docs/FRD.md` | 기능 명세 — 화면별 동작, 입출력, 예외 | 기능 추가·변경 시 |
| `docs/TRD.md` | 기술 요구 — 성능·호환성·보안·품질 기준 | 기술 제약이 바뀔 때 |
| `docs/ARCHITECTURE.md` | 구조 — 디렉터리, 모듈 경계, 데이터 흐름 | 구조 변경 시 |
| `docs/DATA-MODEL.md` | 데이터 논리 모델 | 스키마 변경 시 |
| `docs/DESIGN-BRIEF.md` | 시각 설계 지시서 — 팔레트, 타이포, 모션 | 디자인 방향 변경 시 |
| `docs/adr/*.md` | 되돌리기 어려운 결정과 그 근거 | 새 결정마다 추가 (기존 것은 수정하지 않고 supersede) |
| `docs/research/*.md` | 조사 근거 원본 | 조사할 때마다 추가 |

**ADR은 수정하지 않는다.** 결정이 바뀌면 새 ADR을 쓰고 이전 것의 상태를 `Superseded by ADR-XXXX`로 바꾼다.

## 작업 절차

### 새 기능을 만들 때
1. `docs/FRD.md`에 기능 명세를 먼저 쓴다
2. 되돌리기 어려운 기술 선택이 있으면 ADR을 쓴다
3. 게임 데이터가 필요하면 검증하고 `docs/research/`에 근거를 남긴다
4. 구현
5. `docs/ARCHITECTURE.md`가 낡았으면 갱신

### 게임 데이터를 추가할 때
1. `Docs.json`에서 생성 가능한가? → `scripts/build-data.mjs` 경유
2. 아니면 위키에서 검증 → `source`, `confidence` 필드 포함
3. `src/data/` 아래 JSON에 추가
4. 근거를 `docs/research/`에 기록

### 병렬 리서치를 할 때
- 스코프를 겹치지 않게 나눈다. 겹치면 중복 비용만 든다
- **모델·effort 배분**: 탐색·수집은 `sonnet`/`medium`, 아키텍처 판단·적대적 검증·종합은 기본 모델/`high`
- 조사 결과는 반드시 파일로 남긴다. 에이전트 반환값만 믿지 않는다
- 판단이 갈리는 문제는 독립 제안 → 심사위원 패널 → 합성안 패턴을 쓴다

## 코딩 규약

- 게임 데이터와 사용자 데이터를 섞지 않는다. 게임 데이터는 읽기 전용, 사용자 데이터는 별도 저장소(persistence)
- 사용자 데이터 스키마에는 항상 버전 필드를 둔다. 마이그레이션 없이 구조를 바꾸지 않는다
- 수치 계산에서 부동소수점 비교는 엡실론을 쓴다 (`Math.ceil(x - 1e-9)`)
- 색상은 CSS 커스텀 프로퍼티로만 쓴다. 하드코딩된 hex 금지
- 자원 구분에 **색상만** 쓰지 않는다 — 색각 이상 대응. 항상 텍스트 라벨을 병기한다

## 하지 않는 것

- 계산기 기능으로 기존 도구와 경쟁하지 않는다 (satisfactory-calculator, satisfactorytools가 이미 잘한다 — 링크로 연결한다)
- 세이브 파일을 서버로 보내지 않는다. 모든 파싱은 클라이언트에서
- 검증 못 한 수치를 "대략" 붙여서 쓰지 않는다. 모르면 비워두고 `openQuestions`에 남긴다
- 게임사 브랜딩을 사칭하지 않는다. FICSIT 미학을 차용하되 공식 제품인 척하지 않는다
