# 전면 개편 실행 프로그램

이 문서는 저장소 전체 개편의 상태와 현재 실행 큐를 관리하는 유일한 정본이다. 기존 파일을 유지하는
계획이 아니라 제품 요구사항을 기준으로 소스·데이터·자산·문서를 `유지`, `재작성`, `합병`, `삭제`,
`신규` 중 하나로 판정한다.

## 1. 실행 통제

상태는 `대기`, `진행`, `검증`, `완료`만 사용한다. 구현 파일이 존재해도 요구사항별 자동 검증과 실제
브라우저·도면·데이터 증거가 없으면 완료가 아니다.

현재는 **연구·전환 인벤토리·검증형 구현 병행 단계**다. 대화 중 새 요구는 `PRODUCT`와 관련 기술 정본에
반영하고, 구현 큐는 근거·계약·자동 검증을 갖춘 항목부터 진행한다. 연구가 끝날 때까지 모든 구현을
동결하지 않으며, 반대로 파일이 존재한다는 이유만으로 완료 처리하지 않는다.

## 2. 변경 판정

| 판정 | 조건 |
|---|---|
| 유지 | 현재 구조가 요구사항과 데이터 계약을 충족하고 회귀 증거가 있음 |
| 재작성 | 사용자 과업은 유효하지만 구조·데이터·표현이 요구 수준에 미달 |
| 합병 | 동일 과업이나 데이터가 여러 라우트·컴포넌트에 중복됨 |
| 삭제 | 제품 과업에 기여하지 않거나 검증할 수 없는 구현·데이터·문서 |
| 신규 | 기존 구조로 표현할 수 없는 필수 과업·계약·검증 도구 |

구현 비용, 기간, 토큰, 유료 라이선스는 기술 점수에서 제외한다. 채택하지 않는 판단은
[[capability-evaluation-method-2026]]의 점수와 실제 파일럿 증거로만 한다.

## 3. 게이트

| 게이트 | 상태 | 산출물 | 종료 조건 |
|---|---|---|---|
| G-00 문서 정본 | 완료 | 6개 정본, Research 색인, 최소 속성 | `npm run check:docs` 통과, 중복 정본 0건 |
| G-10 연구 방법 | 완료 | 100점 평가표, 파일럿 규격, 도메인 R-01~12 | 비용 0점, 상위 후보 의무 파일럿 규칙 명시 |
| G-20 전 도메인 심층 연구 | 진행 | 게임·생산·시설·편집기·자산·도면·화면·품질 Research | R-01~12의 출처·점수·파일럿·재검증 조건 완성 |
| G-30 전체 전환 인벤토리 | 진행 | 모든 라우트·컴포넌트·도메인·데이터·자산 판정 | 유지/재작성/합병/삭제/신규 미판정 0건 |
| G-40 완전 실행 계획 | 진행 | 요구사항별 작업·선행조건·검증·증거·배포 순서 | PRODUCT 모든 ID가 하나 이상의 완료 증거에 연결 |
| G-50 구현·검증 | 진행 | 계획의 현재 큐에 있는 코드·데이터·자산 | 큐 단위 검증 후 다음 큐로 이동 |
| G-60 통합 배포 | 대기 | 공개 빌드, 오프라인, 직접 경로, 롤백 | 전체 `verify`와 공개 URL 승인 |

## 4. 연구 프로그램

연구 문서는 출처 채널별로 만들지 않고 책임이 연결된 네 묶음으로 제한한다.

| 연구 묶음 | 범위 | 상태 | 필수 비교 대상 |
|---|---|---|---|
| [[capability-evaluation-method-2026]] | 전 기술·지식·스킬 평가 기준 | 검증 | 100점 점수표와 파일럿 규격 |
| 게임·생산·세이브 근거 | R-01, R-02, R-07 | 진행 | 설치본 Docs, 공식 자료, SCIM, Satisfactory Tools, Modeler, 실제 코퍼스, LP/MILP 후보 |
| 공장·기하·자산·도면 근거 | R-03, R-05, R-06, R-08 | 진행 | 시설 배치 논문·표준, Anders 원본, Figma, 실제 블루프린트, CAD·P&ID·도면 엔진 |
| 제품 인터페이스·품질 근거 | R-04, R-09~R-12 | 진행 | 실제 배포 편집기·지도·가이드·도감, 최신 저장소, 브라우저 표준, Codex 스킬 |

기존 [[architecture-evaluation-2026]]와 [[frontend-landing-technology-2026]]는 위 연구의 입력이지만 전체
기술 결론을 대신하지 않는다.

## 5. 제품 전환 인벤토리 범위

G-30은 다음 항목을 빠짐없이 판정한다.

| 범위 | 현재 확인된 대상 | 반드시 답할 질문 |
|---|---|---|
| 라우트 | `/`, guide, builder, recipes, tools, planner, map, checkup, dex, codex, tech, versions | 정식 과업은 무엇이며 무엇을 합병·삭제·리다이렉트하는가 |
| UI | 계산기 3종, 설계판, 지도 3종, 진행 컴포넌트, 도감 | 상태와 데이터가 중복되는 경계는 무엇인가 |
| 도메인 | `src/lib/*`, `src/domain/factory/*`, `src/state/*` | 순수 도메인, 작업공간, 저장, Worker 경계를 어떻게 재구성하는가 |
| 데이터 | 생성물과 큐레이션 19개 | 출처가 끊겼거나 Claude 근거인 필드를 무엇으로 교체하는가 |
| 자산 | 현재 플래너 21개와 전체 누락 클래스 | Anders 원본, 게임 메시, 신규 렌더를 어떤 프로파일로 통일하는가 |
| 스타일·모션 | 토큰, 공통 셸, 라우트 CSS, GSAP·Lenis·Motion | 콘텐츠 페이지와 전문 작업공간을 어떻게 분리하는가 |
| 검증 | 단위·문서·데이터·시각·접근성·오프라인 | PRODUCT ID마다 어떤 증거가 완료를 입증하는가 |

## 6. 요구사항별 구현 스트림

구현 상세 순서는 G-20과 G-30 이후 확정한다. 스트림 자체는 제품 범위를 빠뜨리지 않기 위해 지금 고정한다.

| 스트림 | 연결 요구사항 | 상태 |
|---|---|---|
| 생산 계산·설계 인계 | PROD-01~04 | 대기 |
| 수동 설계 편집기·상태 | PLAN-01~08, STATE-01 | 진행 |
| 탑뷰·토대·운송 자산 | ASSET-01, PLAN-02~07 | 진행 |
| 벨트·파이프·리프트·깊이 | PLAN-04, PLAN-06~07 | 대기 |
| SVG·PNG 도면 발행 | PLAN-09 | 대기 |
| 세이브 해석·진단 | SAVE-01, CHECK-01 | 대기 |
| 지도·도감·가이드 | MAP-01, DEX-01, GUIDE-01 | 대기 |
| 랜딩·정보 구조·모션 | LAND-01~02, IA-01 | 대기 |
| 디자인·접근성·성능·오프라인 | QUAL-01~06 | 대기 |

## 7. 현재 실행 큐

자산 큐는 `src/data/app/buildings.json`의 `unlockTier` 오름차순으로 처리한다. 같은 티어에서는 추출·전력,
생산, 물류, 저장 순이다. 벨트와 리프트는 Mk별 시각 차이를 보존하고, 메시·재질 해시가 동일하다는 게임
근거가 있을 때만 자산을 공유한다.

1. **완료** — AndersPottemager Satisfactory 원 게시물 21건·매체 42건을 색인하고 원본 37건을 해시 보관했다.
2. 게시물·자산별 재배포·파생 사용 조건을 확정하고 공개 참조와 앱 포함 가능 자산을 분리한다.
3. **파일럿 완료·정식화 진행** — FModel/CUE4Parse로 게임 1.2의 패키지 48,573건을 인덱싱하고
   정적·스켈레탈/VAT 메시·표시등·재질을 추출했다. Blender 5.2 LTS 프리셋과 채굴기·자동 바이오매스
   후보를 만들었다. 다음 조건은 CUE4Parse 의존성 보안 권고 해소와 복합 Anders 도면 시각 승인이다.
4. Anders `Texture 02`의 컨베이어 리프트 21·33번 방향을 게임과 대조하고 Mk.1~6 공통 탑뷰 규격을 확정한다.
5. **제작기 기준본 승인·양산 하네스 진행** — 현재 게임 제작기 메시·재질·Blueprint 조립 기준본을
   `ORTHO/-Z`, 8×10×6m 하드 박스, WebP·PNG SHA-256으로 잠갔다. 직접 제품 렌더를 차단하고 독립
   Blender 계약 검사와 승인 전 격리 영수증을 생성한다. 다음 작업은 그레이팅 투과 표현을 기준본의
   카메라·조립·코너 변경 없이 적용하는 것이다.
6. **그래프 엔지니어링 1차 완료** — 설치본 55,753개 노드·95,236개 간선 증거 DB와 검색·경로·드리프트
   하네스, 포트 인지 방향 다중 그래프 코어, 10,000 노드 반복형 알고리즘, fast-check 속성 테스트,
   68개 런타임 모듈 경계 검사를 도입했다. 다음은 물류 projection과 기존 검증기의 differential 통합이다.
7. 게임·생산·세이브 레퍼런스 제품과 알고리즘 후보를 수집하고 점수화한다.
8. 시설 배치·운송 기하·공정 도면의 원 논문·표준·실제 블루프린트를 수집한다.
9. 가이드·무한 캔버스·CAD·지도·도감·상태·내보내기·QA의 배포 제품과 최신 기술을 점수화한다.
10. R-01~12 결과를 검토해 G-20을 검증 상태로 이동한다.
11. 전체 저장소 전환 인벤토리와 완전 실행 계획을 작성한다.

## 8. 스트림 완료 조건

다음이 모두 충족되기 전에는 G-50과 전체 제품을 완료 처리하지 않는다.

- R-01~12에 공식·원본 근거, 최신 지표, 점수와 실제 파일럿 계획이 있음
- 고별·활성 저장소와 실제 배포 제품을 임의로 제외하지 않았음
- 모든 기존 파일군에 변경 판정이 있음
- PRODUCT의 모든 ID가 구현 작업과 검증 증거에 연결됨
- 삭제·합병 대상의 데이터·URL·사용자 상태 마이그레이션이 정의됨
- 전체 검증, 배포, 오프라인, 롤백 순서가 정의됨
